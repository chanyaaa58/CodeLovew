<h1>My Page</h1>

<%# <%= image_tag user.icon.url, size: '320x240' if user.icon && user.icon.url %></td>
<%= @user.name %><br>
<%= @user.email %><br>
<%= image_tag(@user.icon.url) if @user.icon && @user.icon.url %>

<p><%= link_to 'Edit', edit_user_registration_path(@user.id) %></p>